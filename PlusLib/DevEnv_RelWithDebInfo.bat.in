@ECHO off
REM Hide all your directories from users
SET DIRCMD=0 
SET PATH=${VTK_DIR}/bin/RelWithDebInfo;${ITK_DIR}/bin/RelWithDebInfo;%PATH%
SET PATH=${ULTERIUS_BINARY_DIR}/r;${ICCAPTURING_BIN_DIR}/release;${NDIOAPI_BINARY_DIR};%PATH%
REM SET PATH=${OpenIGTLink_DIR}/bin/RelWithDebInfo;%PATH%
SET PATH=${QT_BINARY_DIR};%PATH%
SET PATH=c:/cygwin/bin;%PATH%
SET SVNCOMMAND=svn.exe
REM Emptying SVN_SSH variable is needed so that AnkhSVN Visual Studio plugin work (without this unset command, it cannot commit)
SET SVN_SSH=

if "%1" == "no-sln" goto End

PLUS.sln

:End